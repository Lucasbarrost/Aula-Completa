
algoritmo "VerificarTemperatura"

var
    temperatura: real

inicio
    escreva("Digite a temperatura em °C: ")
    leia(temperatura)

    se (temperatura > 0) então
        escreva("A temperatura está acima do ponto de congelamento.")

        senao
        
        escreval("A temperatira está abaixo de -0. ")
    fimse
fimalgoritmo
